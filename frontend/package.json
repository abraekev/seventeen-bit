{
  "name": "seventeen-bit",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter='./frontend/*' run dev",
    "build": "pnpm --parallel --filter='./frontend/*' run build",
    "lint": "pnpm --parallel --filter='./frontend/*' run lint"
  }
}
